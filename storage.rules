rules_version = '2';
// TEMP: regras abertas para teste — restringir antes de produção
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
